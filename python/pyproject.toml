[project]
name = "llm-scratch"
version = "0.1.0"
description = "从零构建大模型"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "tiktoken>=0.11.0",
    "torch>=2.8.0",
    "torchvision>=0.23.0",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
